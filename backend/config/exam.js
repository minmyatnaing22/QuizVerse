// Single exam date for every QuizVerse page that shows a countdown.
// ISO calendar date (YYYY-MM-DD). Remaining days = exam date - today, never below 0.
module.exports = {
    EXAM_DATE: "2027-03-10"
};
